## Deferred (do not lose)
- Decide whether mercenary wages are taxable as citizen income (default: not taxable)

## Later
- Map chronicle events (other member; return later)

## Launch Day Checks
- Settlement timer
- AP gain timer
- Confirm the live `war.yml` keeps `war.require_declare_code: true` and the `factions` cog can mint against the live realm
